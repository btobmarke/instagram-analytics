# Instagram分析システム 実装フェーズ計画書

**バージョン**: 1.0
**作成日**: 2026-04-03
**想定総期間**: 約10〜12週間（1人開発）

---

## 1. フェーズ全体マップ

```
Phase 0（1週間）
  └─ 環境構築・プロジェクト初期化

Phase 1A（1週間）
  └─ 認証・アカウント管理基盤

Phase 1B（2週間）
  └─ データ収集バッチ（Collector層 + Fact層）

Phase 1C（2週間）
  └─ KPI計算基盤 + フロント基本画面

Phase 1D（1.5週間）
  └─ AI分析連携

Phase 1E（1.5週間）
  └─ 分析画面・グラフ実装

Phase 1F（1週間）
  └─ 設定画面・管理画面

Phase QA（1週間）
  └─ 結合テスト・バグ修正・デプロイ

Phase 2（別途）
  └─ Webhook対応・高度機能
```

---

## 2. Phase 0: 環境構築（1週間）

### 目標
動くスケルトンを用意して、全Phaseの開発土台を整える

### タスク一覧

| # | タスク | 詳細 |
|---|-------|------|
| 0-1 | Next.jsプロジェクト作成 | `npx create-next-app@latest` (TypeScript, App Router, Tailwind) |
| 0-2 | Supabaseプロジェクト作成 | Supabase Dashboardでプロジェクト作成・接続情報取得 |
| 0-3 | Vercelプロジェクト作成 | GitHubリポジトリと連携 |
| 0-4 | 環境変数設定 | `.env.local` / Vercel環境変数に各種キーを設定 |
| 0-5 | DBマイグレーション環境構築 | `supabase/migrations/` ディレクトリ構成、Supabase CLI設定 |
| 0-6 | 全テーブルのマイグレーション実行 | ER図定義書のDDLをマイグレーションファイルとして作成・実行 |
| 0-7 | KPIマスタデータ投入 | kpi_master の初期データ INSERT |
| 0-8 | Supabase Auth 設定 | メール認証有効化、初期ユーザー作成 |
| 0-9 | ESLint / Prettier / Husky設定 | コード品質ツール設定 |
| 0-10 | 基本的なディレクトリ構成作成 | app/, components/, lib/, types/ の雛形作成 |

### 環境変数一覧

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Claude API
ANTHROPIC_API_KEY=

# Instagram / Meta
META_APP_ID=
META_APP_SECRET=

# App
NEXT_PUBLIC_APP_URL=
CRON_SECRET=               # バッチ認証用シークレット
```

### 完了条件
- [ ] `npm run dev` でNext.jsが起動する
- [ ] Supabaseに全テーブルが作成されている
- [ ] KPIマスタデータが投入されている
- [ ] Vercelへのデプロイが成功する
- [ ] Supabase Authでログインできる

---

## 3. Phase 1A: 認証・アカウント管理基盤（1週間）

### 目標
ログインしてアカウントを登録・管理できる最小構成を完成させる

### タスク一覧

| # | タスク | 実装内容 |
|---|-------|---------|
| 1A-1 | ログイン画面実装 | SCR-01: Supabase Auth UIコンポーネント活用 |
| 1A-2 | 認証ミドルウェア実装 | `middleware.ts`: 未認証時は `/auth/login` にリダイレクト |
| 1A-3 | グローバルナビ実装 | `layout.tsx`: ナビゲーションバーコンポーネント |
| 1A-4 | アカウント一覧API | `GET /api/accounts` |
| 1A-5 | アカウント登録API | `POST /api/accounts` |
| 1A-6 | アカウント更新API | `PUT /api/accounts/{id}` |
| 1A-7 | アカウント削除API | `DELETE /api/accounts/{id}` |
| 1A-8 | アカウント設定画面 | SCR-07: 一覧画面 |
| 1A-9 | アカウント設定詳細画面 | SCR-08: 基本情報タブ + API設定タブ |
| 1A-10 | トークン管理 | ig_account_tokens への保存・取得（暗号化） |
| 1A-11 | 接続テスト機能 | Instagram Graph API への疎通確認 |
| 1A-12 | アカウント選択画面（ダミー） | SCR-02: カード表示（データは空でOK） |

### 重要実装ポイント

**アクセストークンの暗号化:**
```typescript
// lib/crypto.ts
import { createCipheriv, createDecipheriv, randomBytes } from 'crypto';

const ENCRYPTION_KEY = process.env.TOKEN_ENCRYPTION_KEY!; // 32bytes hex

export function encryptToken(token: string): string {
  const iv = randomBytes(16);
  const cipher = createCipheriv('aes-256-cbc', Buffer.from(ENCRYPTION_KEY, 'hex'), iv);
  const encrypted = Buffer.concat([cipher.update(token), cipher.final()]);
  return `${iv.toString('hex')}:${encrypted.toString('hex')}`;
}

export function decryptToken(encryptedToken: string): string {
  const [ivHex, encrypted] = encryptedToken.split(':');
  const iv = Buffer.from(ivHex, 'hex');
  const decipher = createDecipheriv('aes-256-cbc', Buffer.from(ENCRYPTION_KEY, 'hex'), iv);
  return Buffer.concat([decipher.update(Buffer.from(encrypted, 'hex')), decipher.final()]).toString();
}
```

**Supabase クライアント設定:**
```typescript
// lib/supabase/server.ts  （Server Component / API Routes用）
import { createServerClient } from '@supabase/ssr';
import { cookies } from 'next/headers';

export function createSupabaseServerClient() {
  const cookieStore = cookies();
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { get: (name) => cookieStore.get(name)?.value } }
  );
}

// lib/supabase/admin.ts  （バッチ・管理用: RLS無効）
import { createClient } from '@supabase/supabase-js';

export const supabaseAdmin = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
);
```

### 完了条件
- [ ] ログイン / ログアウトが動作する
- [ ] アカウントの登録・編集・削除ができる
- [ ] アクセストークンが暗号化して保存される
- [ ] 接続テストでInstagram APIへの疎通が確認できる

---

## 4. Phase 1B: データ収集バッチ（2週間）

### 目標
Instagram APIからのデータ収集パイプラインを完成させ、DBにデータが溜まり始める状態にする

### タスク一覧

| # | タスク | 実装内容 | 優先度 |
|---|-------|---------|-------|
| 1B-1 | Instagram APIクライアント実装 | `lib/instagram/client.ts` | 最高 |
| 1B-2 | レート制限管理実装 | `X-App-Usage` 監視・制御 | 最高 |
| 1B-3 | バッチ共通ロジック実装 | ログ記録・リトライ・エラーハンドリング | 高 |
| 1B-4 | Supabase Edge Functions 雛形 | 認証・ルーティング共通処理 | 高 |
| 1B-5 | token_refresh_batch | トークン自動更新 | 高 |
| 1B-6 | account_sync_batch | アカウント基本情報同期 | 高 |
| 1B-7 | media_index_batch | 投稿一覧取得 | 最高 |
| 1B-8 | media_insight_batch | 投稿Insight取得（Tier別） | 最高 |
| 1B-9 | account_insight_batch | アカウント日次Insight取得 | 高 |
| 1B-10 | story_insight_batch | ストーリーInsight取得 | 中 |
| 1B-11 | comment_sync_batch | コメント同期 | 低 |
| 1B-12 | pg_cron スケジュール設定 | 各バッチのcron設定 | 高 |
| 1B-13 | backfill_batch | 手動再取得バッチ | 低 |
| 1B-14 | cleanup_batch | ログクリーンアップ | 低 |

### 実装の優先順序

```
Week 1:
  1B-1（APIクライアント）→ 1B-2（レート制限）→ 1B-3（共通処理）
  → 1B-7（media_index）→ 1B-8（media_insight）→ 1B-12（cron設定）

Week 2:
  1B-5（token_refresh）→ 1B-6（account_sync）→ 1B-9（account_insight）
  → 1B-10（story_insight）→ 1B-11（comment_sync）
  → 1B-13（backfill）→ 1B-14（cleanup）
```

### Instagram APIクライアント設計

```typescript
// lib/instagram/client.ts
export class InstagramClient {
  private accessToken: string;
  private accountId: string;
  private apiVersion = 'v22.0';
  private baseUrl = 'https://graph.instagram.com';

  async getMedia(params: GetMediaParams): Promise<MediaResponse> { ... }
  async getMediaInsights(mediaId: string, metrics: string[]): Promise<InsightResponse> { ... }
  async getAccountInsights(metrics: string[], period: string): Promise<InsightResponse> { ... }
  async getProfile(): Promise<ProfileResponse> { ... }

  private async fetch(endpoint: string, params: Record<string, string>) {
    const url = new URL(`${this.baseUrl}/${this.apiVersion}${endpoint}`);
    // パラメータ付加
    const response = await fetch(url.toString());

    // X-App-Usage を記録
    const usage = response.headers.get('X-App-Usage');
    await this.logRateUsage(usage);

    // レート制限チェック
    if (this.isRateLimited(usage)) {
      throw new RateLimitError('Rate limit threshold exceeded');
    }
    return response.json();
  }
}
```

### Edge Function の基本構造

```typescript
// supabase/functions/media-index-batch/index.ts
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

serve(async (req: Request) => {
  // CRON_SECRET で認証
  const authHeader = req.headers.get('Authorization');
  if (authHeader !== `Bearer ${Deno.env.get('CRON_SECRET')}`) {
    return new Response('Unauthorized', { status: 401 });
  }

  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  );

  // バッチログ開始
  const { data: jobLog } = await supabase
    .from('batch_job_logs')
    .insert({ job_name: 'media_index_batch', status: 'running' })
    .select().single();

  try {
    // メイン処理
    const result = await runMediaIndexBatch(supabase);

    // バッチログ完了
    await supabase.from('batch_job_logs').update({
      status: 'success',
      records_processed: result.processed,
      finished_at: new Date().toISOString()
    }).eq('id', jobLog.id);

    return new Response(JSON.stringify(result), { status: 200 });
  } catch (error) {
    await supabase.from('batch_job_logs').update({
      status: 'failed',
      error_message: error.message,
      finished_at: new Date().toISOString()
    }).eq('id', jobLog.id);

    return new Response(JSON.stringify({ error: error.message }), { status: 500 });
  }
});
```

### 完了条件
- [ ] 全バッチがpg_cronで自動実行される
- [ ] ig_media にデータが蓄積されている
- [ ] ig_media_insight_fact にスナップショットが時系列で蓄積されている
- [ ] ig_account_insight_fact に日次データが蓄積されている
- [ ] レート制限超過時に自動でスキップする動作が確認できる
- [ ] バッチエラー時にbatch_job_logsにエラー記録が残る

---

## 5. Phase 1C: KPI計算基盤 + 基本画面（2週間）

### 目標
KPI計算が動き、投稿一覧・アカウント選択画面が実際のデータで表示される

### タスク一覧

| # | タスク | 実装内容 |
|---|-------|---------|
| 1C-1 | kpi_calc_batch 実装 | Type A, B, C の全KPI計算 |
| 1C-2 | target_compare_batch 実装 | 目標値比較・進捗更新 |
| 1C-3 | アカウント選択画面 実装 | SCR-02: 実データ表示 |
| 1C-4 | 投稿一覧API 実装 | `GET /api/accounts/{id}/posts` |
| 1C-5 | 投稿一覧画面 実装 | SCR-03: チェックボックス・フィルタ |
| 1C-6 | 投稿詳細API 実装 | `GET /api/accounts/{id}/posts/{postId}` |
| 1C-7 | 投稿詳細画面（指標値部分） | SCR-04: 指標値カード表示 |
| 1C-8 | アカウント分析API 実装 | `GET /api/accounts/{id}/analysis` |
| 1C-9 | アカウント分析画面（基本情報・KPI部分） | SCR-06: KPIサマリーカード |
| 1C-10 | KPI設定タブ実装 | SCR-08: KPI目標値の登録・編集 |
| 1C-11 | バッチ管理画面 実装 | SCR-10: 状態確認・手動実行 |

### KPI計算バッチの実装方針

```typescript
// KPI計算エンジン（メトリクスタイプ別に処理を分岐）
async function calcKpi(kpi: KpiMaster, mediaId?: string, accountId?: string) {
  switch (kpi.capability_type) {
    case 'DIRECT_API':
      return await calcDirectApiKpi(kpi, mediaId, accountId);
    case 'DERIVED':
      return await calcDerivedKpi(kpi, mediaId, accountId);
    case 'UNAVAILABLE_NOW':
      return { value: null, source_status: 'unsupported' };
  }
}

async function calcDerivedKpi(kpi: KpiMaster, mediaId?: string) {
  // formula_type に応じた計算
  switch (kpi.formula_type) {
    case 'ratio':
      const num = await getLatestMetric(mediaId, kpi.numerator_source);
      const den = await getLatestMetric(mediaId, kpi.denominator_source);
      if (den === null || den === 0) return { value: null, source_status: 'partial' };
      return { value: num / den * 100, source_status: 'complete' };
    case 'delta':
      // 前期との差分計算
      ...
  }
}
```

### 完了条件
- [ ] kpi_result にデータが計算・蓄積されている
- [ ] kpi_progress に目標比較データが入っている
- [ ] アカウント選択画面に実際のアカウントが表示される
- [ ] 投稿一覧画面に実際の投稿が表示される
- [ ] 投稿詳細画面に指標値カードが表示される
- [ ] アカウント分析画面にKPIサマリーが表示される
- [ ] バッチ管理画面でバッチ状態が確認できる

---

## 6. Phase 1D: AI分析連携（1.5週間）

### 目標
Claude API を使ったAI分析機能を全画面に実装する

### タスク一覧

| # | タスク | 実装内容 |
|---|-------|---------|
| 1D-1 | Claude APIクライアント実装 | `lib/claude/client.ts` |
| 1D-2 | 投稿AI分析API 実装 | `POST /api/accounts/{id}/posts/{postId}/ai-analysis` |
| 1D-3 | 投稿詳細画面 AI分析ボタン | SCR-04: ボタン・結果表示 |
| 1D-4 | 投稿比較AI分析API 実装 | `POST /api/accounts/{id}/posts/ai-analysis-comparison` |
| 1D-5 | 投稿分析画面 AI分析ボタン | SCR-05: ボタン・結果表示 |
| 1D-6 | ai_weekly_batch 実装 | 週次AI分析バッチ |
| 1D-7 | ai_monthly_batch 実装 | 月次AI分析バッチ |
| 1D-8 | アカウント分析画面 履歴タブ | SCR-06: 週次・月次分析履歴表示 |
| 1D-9 | プロンプト設定画面 実装 | SCR-09: 全4タブ + アルゴリズム取得 |
| 1D-10 | アルゴリズム取得API 実装 | `POST /api/settings/prompts/fetch-algorithm` |

### Claude APIクライアント設計

```typescript
// lib/claude/client.ts
import Anthropic from '@anthropic-ai/sdk';

const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

export async function analyzePost(params: {
  postData: PostData;
  insights: InsightData;
  prompt: string;
  accountStrategy: string;
}): Promise<string> {
  const systemPrompt = `あなたはInstagramマーケティングの専門家です。
以下の分析観点に基づいて投稿を分析してください。

【分析観点】
${params.prompt}

【アカウント戦略】
${params.accountStrategy}`;

  const userMessage = `以下の投稿データを分析してください：
${JSON.stringify(params.postData, null, 2)}

【指標値】
${JSON.stringify(params.insights, null, 2)}`;

  const response = await anthropic.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 2048,
    system: systemPrompt,
    messages: [{ role: 'user', content: userMessage }]
  });

  return response.content[0].type === 'text' ? response.content[0].text : '';
}

export async function fetchLatestAlgorithmInfo(): Promise<string> {
  // Web検索ツールを使ってInstagramの最新アルゴリズム情報を取得
  const response = await anthropic.messages.create({
    model: 'claude-sonnet-4-6',
    max_tokens: 2048,
    tools: [{ type: 'web_search_20250305', name: 'web_search' }],
    messages: [{
      role: 'user',
      content: `2026年時点のInstagramアルゴリズムの最新情報を調査して、
      マーケティング担当者が知っておくべきポイントを日本語で整理してください。
      フィード、リール、ストーリーそれぞれについて説明してください。`
    }]
  });

  return extractTextFromResponse(response);
}
```

### ストリーミング対応（AI分析のUX向上）

```typescript
// app/api/accounts/[accountId]/posts/[postId]/ai-analysis/route.ts
export async function POST(req: Request) {
  // Streaming response でリアルタイムにテキストを返す
  const stream = new ReadableStream({
    async start(controller) {
      const response = await anthropic.messages.stream({ ... });
      for await (const chunk of response) {
        if (chunk.type === 'content_block_delta') {
          controller.enqueue(new TextEncoder().encode(chunk.delta.text));
        }
      }
      controller.close();
    }
  });

  return new Response(stream, {
    headers: { 'Content-Type': 'text/event-stream' }
  });
}
```

### 完了条件
- [ ] 投稿詳細画面でAI分析が実行できる（ストリーミング表示）
- [ ] 投稿分析画面で比較AI分析が実行できる
- [ ] 週次・月次バッチのAI分析が自動実行される
- [ ] アカウント分析画面に分析履歴が表示される
- [ ] プロンプト設定画面で4種類のプロンプトが編集できる
- [ ] 最新アルゴリズム取得ボタンが動作する

---

## 7. Phase 1E: 分析画面・グラフ実装（1.5週間）

### 目標
投稿一覧・投稿分析画面のグラフ・比較機能を完成させる

### タスク一覧

| # | タスク | 実装内容 |
|---|-------|---------|
| 1E-1 | Recharts セットアップ | `npm install recharts` |
| 1E-2 | InsightLineChart コンポーネント実装 | 折れ線グラフ基本実装 |
| 1E-3 | MetricSelector コンポーネント実装 | 指標チェックボックス |
| 1E-4 | 時間軸切り替え実装 | 1時間/12時間/1日の切り替えロジック |
| 1E-5 | 投稿比較グラフAPIの実装 | スナップショットデータをグラフ用に整形 |
| 1E-6 | 投稿分析画面 グラフ部分実装 | SCR-05: グラフエリア |
| 1E-7 | 投稿分析画面 タブ部分実装 | SCR-05: 投稿内容・指標値タブ |
| 1E-8 | PostMediaSlider コンポーネント | カルーセルスライダー実装 |
| 1E-9 | 投稿詳細画面 メディア表示部分 | SCR-04: 画像/動画スライダー |
| 1E-10 | レスポンシブ対応 | モバイル表示の調整 |

### グラフデータの整形ロジック

```typescript
// lib/utils/chart.ts

// DBのスナップショットデータをRechartsが読めるフォーマットに変換
export function formatSnaphotsForChart(
  snapshots: MediaInsightFact[],
  grain: 'hourly' | '12h' | 'daily',
  metrics: string[]
): ChartDataPoint[] {
  // grain に応じてデータを集約・間引き
  const grouped = groupByTimeGrain(snapshots, grain);

  return grouped.map(group => ({
    time: group.time,
    ...metrics.reduce((acc, metric) => ({
      ...acc,
      [metric]: group.values[metric] ?? null
    }), {})
  }));
}

// グラフの色パレット（指標ごとに固定色）
export const METRIC_COLORS: Record<string, string> = {
  reach: '#3B82F6',         // blue
  impressions: '#8B5CF6',    // purple
  likes: '#EF4444',          // red
  comments: '#F59E0B',       // amber
  saved: '#10B981',          // emerald
  shares: '#06B6D4',         // cyan
  engagement_rate: '#F97316', // orange
  save_rate: '#84CC16',      // lime
};
```

### 完了条件
- [ ] 投稿分析画面でグラフが表示される
- [ ] 指標チェックボックスで表示指標が切り替えられる
- [ ] 時間軸（1時間/12時間/1日）が切り替えられる
- [ ] 複数投稿を最大3列で横並び表示できる
- [ ] 投稿内容タブ・指標値タブが動作する
- [ ] カルーセル投稿でスライダーが動作する

---

## 8. Phase 1F: 設定画面・管理画面（1週間）

### 目標
設定系画面を完成させてシステム全体を操作可能な状態にする

### タスク一覧

| # | タスク | 実装内容 |
|---|-------|---------|
| 1F-1 | アカウント設定画面 完成 | SCR-07: 一覧画面の詳細実装 |
| 1F-2 | アカウント設定詳細 完成 | SCR-08: 全4タブの実装 |
| 1F-3 | 戦略設定タブ 実装 | SCR-08: 戦略テキスト入力・保存 |
| 1F-4 | バッチ管理画面 完成 | SCR-10: 手動実行・リアルタイムログ |
| 1F-5 | バッチ手動実行API 実装 | `POST /api/settings/batch/{jobName}/run` |
| 1F-6 | エラー表示コンポーネント | トースト通知・エラーバウンダリ |
| 1F-7 | ローディング状態の統一 | Suspense / loading.tsx の設定 |
| 1F-8 | 404 / エラーページ実装 | error.tsx / not-found.tsx |

### 完了条件
- [ ] アカウントの全設定項目が保存できる
- [ ] バッチを手動実行できる
- [ ] バッチログがリアルタイムで更新される
- [ ] エラー時にわかりやすいメッセージが表示される

---

## 9. Phase QA: テスト・デプロイ（1週間）

### 目標
本番環境で安定稼働できる状態に整える

### タスク一覧

| # | タスク | 内容 |
|---|-------|------|
| QA-1 | 結合テスト | 全画面の動作確認（チェックシートで実施） |
| QA-2 | バッチ動作確認 | 全バッチが正常にスケジュール実行される確認 |
| QA-3 | レート制限テスト | APIレート制限超過時の動作確認 |
| QA-4 | トークン期限切れテスト | トークン無効時の動作確認 |
| QA-5 | エラーケーステスト | 各種エラー時のUI表示確認 |
| QA-6 | パフォーマンス確認 | 投稿一覧・グラフの表示速度確認 |
| QA-7 | 本番Vercelデプロイ | 本番環境への最終デプロイ |
| QA-8 | Supabase本番設定 | RLSポリシー確認・本番DB設定 |
| QA-9 | pg_cron本番設定 | 本番環境でのバッチスケジュール設定 |

---

## 10. Phase 2: 将来機能（別途計画）

Phase 1完成後に追加する機能の候補。

| 機能 | 概要 | 優先度 |
|-----|------|-------|
| Webhook対応 | Meta WebhookでコメントをリアルタイムI受信 | 高 |
| ダッシュボード強化 | アカウント横断サマリーダッシュボード | 高 |
| アラート機能 | KPI達成率低下時のメール/通知 | 中 |
| レポートエクスポート | PDF/Excelでのレポート出力 | 中 |
| 競合アカウント分析 | 他アカウントとのベンチマーク比較 | 低 |
| スケジュール投稿連携 | 投稿スケジュール管理 | 低 |
| タグ・ハッシュタグ分析 | タグ別のパフォーマンス分析 | 低 |

---

## 11. 技術スタック詳細

### フロントエンド

| カテゴリ | ライブラリ | バージョン | 用途 |
|---------|----------|-----------|------|
| フレームワーク | Next.js | 15 | App Router |
| 言語 | TypeScript | 5.x | 型安全 |
| スタイリング | Tailwind CSS | 3.x | UIスタイリング |
| UIコンポーネント | shadcn/ui | latest | ベースUI |
| グラフ | Recharts | 2.x | 折れ線グラフ |
| 状態管理 | Zustand | 4.x | グローバル状態（選択投稿など） |
| フォーム | React Hook Form | 7.x | フォーム管理 |
| バリデーション | Zod | 3.x | スキーマバリデーション |
| HTTPクライアント | SWR | 2.x | データフェッチ・キャッシュ |

### バックエンド

| カテゴリ | ライブラリ/サービス | 用途 |
|---------|------------------|------|
| DB | Supabase (PostgreSQL) | データストア |
| 認証 | Supabase Auth | ユーザー認証 |
| バッチ実行 | Supabase Edge Functions + pg_cron | バッチスケジューリング |
| AI | Anthropic Claude API (claude-sonnet-4-6) | AI分析 |
| 暗号化 | Node.js crypto | トークン暗号化 |

### インフラ

| カテゴリ | サービス | 用途 |
|---------|---------|------|
| フロント | Vercel | Next.jsホスティング |
| DB/バッチ | Supabase | PostgreSQL + Edge Functions + pg_cron |

---

## 12. 開発スケジュールサマリー

```
Week 1:    Phase 0   環境構築・DBマイグレーション
Week 2:    Phase 1A  認証・アカウント管理基盤
Week 3:    Phase 1B  バッチ実装（前半：APIクライアント + media系）
Week 4:    Phase 1B  バッチ実装（後半：account系 + cron設定）
Week 5:    Phase 1C  KPI計算基盤（前半：バッチ実装）
Week 6:    Phase 1C  KPI計算基盤（後半：基本画面実装）
Week 7:    Phase 1D  AI分析連携
Week 8:    Phase 1D  AI分析連携（後半：バッチ + プロンプト設定画面）
Week 9:    Phase 1E  分析画面・グラフ実装
Week 10:   Phase 1F  設定画面・管理画面 完成
Week 11:   Phase QA  テスト・バグ修正・本番デプロイ
Week 12:   バッファ  追加対応・調整
```

---

## 13. 実装開始前チェックリスト

### Meta Developer設定（事前準備）
- [ ] Meta Developers でアプリ作成
- [ ] Instagram Graph API の利用申請・審査（審査に時間がかかる場合あり）
- [ ] 必要スコープの申請:
  - `instagram_business_basic`
  - `instagram_business_manage_insights`
  - `instagram_business_content_publish` （Phase2で必要）
- [ ] Long-Lived Access Token の発行手順確認
- [ ] テスト用ビジネスアカウントの用意

### Supabase設定（事前準備）
- [ ] Supabase プロジェクト作成（リージョン: ap-northeast-1）
- [ ] pg_cron 拡張機能が有効になっているか確認
- [ ] pg_net 拡張機能が有効になっているか確認（Edge Functions呼び出し用）
- [ ] Row Level Security (RLS) 設計の確認

### Anthropic設定（事前準備）
- [ ] Claude API キーの取得
- [ ] Web検索ツールの利用可否確認（`web_search_20250305`）
- [ ] APIの利用上限・課金設定の確認
```
